import React from 'react';
import Layout from '../components/Layout';

const Account = ({ location: { pathname } }) => (
  <Layout pathname={pathname}>
    <h1>This is where your account will go</h1>
    <i>Soon to come</i>
  </Layout>
);

export default Account;
